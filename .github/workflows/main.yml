name: Consolidated Exam Workflow
on: [push, workflow_dispatch]
jobs:
  exam-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: 24f1000124@ds.study.iitm.ac.in
        run: echo "Verifying email step"
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-4d0a528
      - name: prime-cache-4d0a528
        run: echo "Cache primed with key cache-4d0a528"
